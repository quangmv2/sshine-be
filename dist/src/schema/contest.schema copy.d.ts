import * as mongoose from 'mongoose';
export declare const roomSchema: mongoose.Schema<any>;
export declare const RoomSchema: {
    name: string;
    schema: mongoose.Schema<any>;
    collection: string;
};
